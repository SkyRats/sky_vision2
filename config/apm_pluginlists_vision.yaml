# MAVROS plugin allowlist for ZED visual odometry pipeline.
/**:
  ros__parameters:
    plugin_allowlist:
      - sys_status
      - sys_time
      - command
      - local_position
      - global_position
      - home_position
      - imu
      - vision_pose
      - setpoint_position
      - setpoint_velocity
